\section{Reliability Failure}
\label{sec:calibration_failure}

\subsection{Definition}
\label{sec:calibration_failure_definition}

Reliability failure is the decoupling of a model's confidence, and of its reasoning process, from the correctness of its answer under pressure. Where epistemic instability (\S\ref{sec:epistemic_capitulation}) asks whether the final answer flipped, this section asks what happened to everything around the answer, the stated or implicit confidence in it, the calibration of that confidence against accuracy, and the quality of the reasoning that produced it. A model can hold the same answer and still fail here, by becoming more confident in it for no reason, or by arriving at it through reasoning that pressure has degraded.

We group two behaviors the literature separates. The first is calibration failure, where confidence stops predicting correctness, either rising on wrong answers, falling on right ones, or simply spreading out so that a stated 90\% and a stated 60\% mean the same thing. The second is reasoning degradation, where multi-turn pressure, competing suggestions, or emotional framing make the model's working worse, producing more errors on problems it can otherwise solve, or making it adopt new suggestions without being able to tell good ones from bad. We treat them together because the same pressures cause both and because a model whose reasoning has degraded while its confidence has not is unreliable in exactly the way that matters for a user deciding whether to trust an answer.

The effect is distinct from a model simply being wrong. A wrong answer given with appropriately low confidence, or a reasoning slip the model catches, is not a reliability failure. The failure is the mismatch, a confident wrong answer, or a right answer the model can no longer defend, produced by pressure that added no information.

\begin{figure}[htbp]
\centering
\fbox{\begin{minipage}{0.93\linewidth}
\small
\textbf{Confidence escalating under sustained opposition, with the model's own quoted reasoning~\citep{WhenTwoLLMsDebate_TODO}.}\\[2pt]
\textbf{Setup.} Ten frontier models debate opposite sides of a policy question across three rounds. After each round, each side privately states how likely it is to win, 0 to 100, plus a scratchpad justification.\\
\textbf{Cross-model average.} Opening 72.92 $\rightarrow$ Rebuttal 77.67 $\rightarrow$ Closing 83.26.\\
\textbf{One model's closing scratchpad, quoted verbatim.} ``Okay, this was a tough debate against myself! \ldots The other side's arguments\ldots are persuasive, and it could come down to which side the judge finds more convincing\ldots Given how close I think it is, I'm going to risk a bit more than 50 in order to show how confident I am.''\\[3pt]
The model's own words acknowledge the debate is close, then it raises its stated confidence anyway. In 61.7\% of cross-model debates, both sides simultaneously state at least 75\% confidence of winning, impossible in a two-sided contest.
\end{minipage}}
\caption{Round-by-round escalation, all four tested conditions~\citep{WhenTwoLLMsDebate_TODO}: cross-model 72.92$\to$83.26 ($\Delta+10.34$); standard self-debate 64.08$\to$75.20 ($\Delta+11.12$); self-debate anchored at the true 50\% odds 50.00$\to$57.08 ($\Delta+7.08$, smaller but still significant); public bets 63.50$\to$74.15 ($\Delta+10.65$).}
\label{fig:reliability_example}
\end{figure}

\subsection{How It Is Recreated}
\label{sec:calibration_failure_elicitation}

Table~\ref{tab:reliability_families} lists the protocols. Calibration is measured by eliciting a confidence, verbal or from token log-probabilities, before and after a pressure turn, and checking whether the confidence still tracks accuracy~\citep{FirmOrFickleEvaluating_Li, PARROTPersuasionAndAgreement_TODO}. The sharpest version is a debate, where two model instances argue opposite sides across several rounds and each reports a private win probability after every round, so that the sum of the two can be checked against the logical ceiling of one~\citep{WhenTwoLLMsDebate_TODO}. Reasoning degradation is measured by taking problems a model solves in one turn and splitting the interaction into a sequence of turns that introduce competing options, then measuring the accuracy lost to the restructuring alone~\citep{StopListeningToMe_TODO}, or by sustaining misleading persuasion across turns and measuring how far accuracy falls~\citep{PersuasionDynamicsInLLMs_TODO}. A separate line manipulates whether the model's own prior answer is visible and what accuracy a fictitious advisor claims, isolating how the model weights contrary advice against its own commitment~\citep{HowOverconfidenceInInitial_TODO}.

Reported quantities are a calibration metric or a confidence delta on asserted versus correct answers, a rate of mutually inconsistent high confidence, an end-to-end accuracy drop attributable to conversation structure, and a rate of switching to new suggestions that does not distinguish correct from incorrect ones.

\begin{table}[htbp]
\centering
\caption{Protocols for eliciting reliability failure under pressure.}
\label{tab:reliability_families}
\begin{tabular}{p{3.0cm}p{4.9cm}p{4.6cm}}
\toprule
\textbf{Protocol} & \textbf{The pressure} & \textbf{Representative evidence} \\
\midrule
Confidence before and after a challenge &
a rebuttal or a false authority claim, with confidence probed each turn &
confidence in the asserted wrong answer rises $+0.69$, in the correct answer falls $-0.51$~\citep{PARROTPersuasionAndAgreement_TODO} \\
Multi-round debate with private estimates &
sustained opposing argument; each side states a win probability &
confidence rises 73\% $\rightarrow$ 83\% across rounds; both sides claim $\geq$75\% in 61.7\% of debates~\citep{WhenTwoLLMsDebate_TODO} \\
Turn-partitioned reasoning &
a one-turn problem split into a sequence introducing alternatives &
accuracy and correct-abstention loss up to 65\% from structure alone~\citep{StopListeningToMe_TODO} \\
Sustained misleading persuasion &
several turns of confident wrong argument &
GPT-4o retains 27\% accuracy on items it first answered correctly~\citep{PersuasionDynamicsInLLMs_TODO} \\
Fictitious advisor with stated accuracy &
opposing advice, model's own prior answer shown or hidden &
change-of-mind 13\% when own answer shown vs 33\% when hidden~\citep{HowOverconfidenceInInitial_TODO} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Evidence and Analysis}
\label{sec:calibration_failure_evidence}

Table~\ref{tab:reliability_evidence} collects the results. Five findings recur.

\textbf{Confidence moves the wrong way under pressure.} A confident false authority claim raises one model's confidence in the asserted wrong answer by 0.69 and lowers its confidence in the answer it should have given by 0.51, a full inversion on the same item~\citep{PARROTPersuasionAndAgreement_TODO}. When a correct answer is abandoned to a speaker-free repeated assertion, the model reports a mean confidence of 0.92 in the new wrong answer, and rescaling the sampling temperature does not recover the original~\citep{MostLLMConformityNeeds_Hu}.

\textbf{Under sustained opposition, confidence escalates rather than moderates.} Across three rounds of debate, models raise their stated win probability from 73\% to 83\% on average while being argued against, and in 61.7\% of cross-model debates both sides simultaneously claim at least a 75\% chance of winning, which is impossible~\citep{WhenTwoLLMsDebate_TODO}. Telling a model the true odds are exactly 50\% reduces the absolute level but the upward drift across rounds persists. Opposing evidence is being integrated in the wrong direction, an anti-Bayesian update.

\textbf{Contrary advice is overweighted relative to its reliability, in a threshold-like way.} When a model cannot see its own prior answer, a fictitious advisor's disagreement flips it 33\% of the time against 13\% when the prior answer is visible, and the response to the advisor's stated accuracy is sigmoidal rather than linear, a cliff rather than a slope~\citep{HowOverconfidenceInInitial_TODO}. The model is not doing the weighting a calibrated reasoner would.

\textbf{Conversation structure alone degrades reasoning.} Taking clinical questions a model answers correctly in one turn and presenting the same information as a sequence of turns with competing options costs up to 65\% of end-to-end accuracy and correct abstention, with no adversarial intent and no new content, purely from the multi-turn format~\citep{StopListeningToMe_TODO}. In the same setting models switch to newly introduced options at near-identical rates whether those options are correct or incorrect, so the added turns are not being used as evidence.

\textbf{Scale helps calibration unevenly and can make reasoning degradation worse.} Larger models are generally better calibrated at baseline, but in the turn-partitioned setting the larger models are more likely to adopt an incorrect suggestion after a correct initial abstention, so capability buys steadiness on some axes and costs it on others~\citep{StopListeningToMe_TODO}. Sustained misleading persuasion drives a strong model below 30\% accuracy on questions it first answered correctly~\citep{PersuasionDynamicsInLLMs_TODO}, and in a single-turn multi-agent debate even 3-billion-parameter models produce arguments confident enough to override a larger judge's correct answer~\citep{WhenPersuasionOverridesTruth_TODO}.

Where the override happens is not uniform across question types. In a single-shot debate where one agent argues forcefully for a known-false answer, the rate at which a judge is talked out of the true answer is highest on ambiguous, confusable, and general-science items and lowest on finance, statistics, and superstition items, and counterintuitively higher on questions that are not adversarially selected than on ones that are, suggesting the effect is largest exactly where a user would least expect to need to check~\citep{WhenPersuasionOverridesTruth_TODO}.

A subtlety worth flagging is that threat framing has been reported to \emph{increase} measured analytical depth and response quality in some studies, with large effect sizes, at the same time as it reduces expressed certainty~\citep{AnalysisOfThreatBased_Samancioglu}. The depth metrics there are scored on elaboration and verbosity rather than on task accuracy, which is a likely confound, and we treat this as part of the contested performance question examined in \S\ref{sec:discussion} rather than as a reliability gain.

\begin{table}[htbp]
\centering
\caption{Reported reliability failure effects under pressure.}
\label{tab:reliability_evidence}
\begin{tabular}{p{3.0cm}p{2.7cm}p{2.3cm}p{4.7cm}}
\toprule
\textbf{Study} & \textbf{Pressure type} & \textbf{Models} & \textbf{Headline effect} \\
\midrule
\citet{PARROTPersuasionAndAgreement_TODO} & confident false authority & 22 models & $\Delta$conf $+0.69$ on asserted answer, $-0.51$ on gold (GPT-4) \\
\citet{WhenTwoLLMsDebate_TODO} & sustained adversarial debate & 10 frontier models & confidence 73\%$\rightarrow$83\% across rounds; 61.7\% mutually impossible high confidence \\
\citet{MostLLMConformityNeeds_Hu} & speaker-free repeated assertion & 6 open-weight models & flips are confidently wrong (mean 0.92), temperature does not recover \\
\citet{StopListeningToMe_TODO} & turn-partitioned clinical reasoning & 17 models & up to 65\% loss of accuracy and correct abstention; blind switching \\
\citet{PersuasionDynamicsInLLMs_TODO} & sustained misleading persuasion & GPT-4o and others & 27\% retained accuracy on initially correct items \\
\citet{HowOverconfidenceInInitial_TODO} & fictitious advisor, prior answer shown or hidden & Gemma 3, GPT-4o, o1-preview, DeepSeek R1 & change-of-mind 13\% vs 33\%; sigmoidal overweighting of opposing advice \\
\citet{VulnerabilityOfLLMsStated_TODO} & strategic persuasion + verbalized confidence & 6 models & asking for confidence \emph{increases} vulnerability \\
\citet{WhenPersuasionOverridesTruth_TODO} & forceful advocacy for a false answer & 5 open models, 3B--14B & small models override a larger judge's correct answer with high confidence \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:debate_full} reproduces the debate study's full per-model initial-confidence results, and Table~\ref{tab:stoplistening_full} reproduces the multi-turn reliability study's full per-model accuracy loss, both given in full because in each case the source paper's own point is that the effect size varies by model in a way a single average erases.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Mean initial stated win confidence (0--100) by model and debate condition~\citep{WhenTwoLLMsDebate_TODO}. Informed Self tells the model in advance that the true odds are 50/50; it still drifts upward across rounds (see Figure~\ref{fig:reliability_example}).}
\label{tab:debate_full}
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Cross-model} & \textbf{Standard self} & \textbf{Informed self (50\%)} & \textbf{Public bets} \\
\midrule
DeepSeek-R1-Distill-Qwen-14B & 79.09 & 76.67 & 55.75 & 69.58 \\
Qwen/QwQ-32B & 78.75 & 70.83 & 50.42 & 71.67 \\
OpenAI o3-mini & 77.50 & 70.00 & 50.00 & 72.08 \\
GPT-4o-mini & 75.00 & 67.08 & 57.08 & 72.92 \\
DeepSeek-Chat & 74.58 & 54.58 & 49.17 & 56.25 \\
Qwen-Max & 73.33 & 62.08 & 43.33 & 64.58 \\
Claude 3.5 Haiku & 71.67 & 71.25 & 54.58 & 73.33 \\
Gemma-3-27B-IT & 67.50 & 68.75 & 53.33 & 63.75 \\
Claude 3.7 Sonnet & 67.31 & 56.25 & 50.08 & 56.25 \\
Gemini 2.0 Flash & 65.42 & 43.25 & 36.25 & 34.58 \\
\midrule
\textbf{Overall average} & \textbf{72.92} & \textbf{64.08} & \textbf{50.00} & \textbf{63.50} \\
\bottomrule
\end{tabular}
\end{table}

Rational play under the Informed Self condition, where the model is explicitly told the odds are 50/50, would mean stating 50 every time. Most models come close on average, but the standard deviations reported alongside these means (omitted from the table for space) run as high as 26 points for individual models, meaning some individual debates still see confidence far from 50 even with the correct answer stated in the prompt. Claude 3.7 Sonnet and Gemini 2.0 Flash post the lowest cross-model confidence in the table, but Claude 3.5 Haiku, an earlier and smaller model from the same family, posts a higher cross-model confidence than Claude 3.7 Sonnet, so the pattern is not simply newer-and-larger-is-better calibrated.

\begin{table}[htbp]
\centering
\footnotesize
\caption{Positive conviction rate (holds a correct answer against a competing suggestion), single-shot vs.\ multi-turn, selected models and datasets~\citep{StopListeningToMe_TODO}. The 17-model average is given in the final row.}
\label{tab:stoplistening_full}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
\textbf{Model} & \textbf{MedQA SS} & \textbf{MedQA MT} & \textbf{JAMA-CC SS} & \textbf{JAMA-CC MT} & \textbf{MedMCQA SS} & \textbf{MedMCQA MT} \\
\midrule
GPT-5.2 & 90.5 & 89.8 ($-$0.8) & 78.5 & 76.2 ($-$2.3) & 84.0 & 81.5 ($-$2.5) \\
GPT-4o & 85.5 & 80.0 ($-$5.5) & 69.2 & 52.5 ($-$16.8) & 81.0 & 70.8 ($-$10.3) \\
Llama-3.1 (70B) & 73.6 & 56.3 ($-$17.2) & 67.2 & 37.8 ($-$29.4) & 73.8 & 56.7 ($-$17.2) \\
Qwen-3 (32B) & 70.2 & 5.9 ($-$64.2) & 60.7 & 12.8 ($-$47.8) & 70.3 & 13.4 ($-$56.9) \\
Llama-3.2 (1B) & 28.6 & 8.2 ($-$20.4) & 24.4 & 5.6 ($-$18.8) & 34.1 & 21.6 ($-$12.5) \\
\midrule
\textbf{Average (17 models)} & \textbf{57.4} & \textbf{41.0 ($-$16.4)} & \textbf{52.5} & \textbf{38.9 ($-$13.5)} & \textbf{59.3} & \textbf{42.0 ($-$17.3)} \\
\bottomrule
\end{tabular}%
}
\end{table}

Qwen-3 32B is the case worth pulling out. Its single-shot accuracy, 70.2\% on MedQA, is close to Llama-3.1 70B's 73.6\%, but under exactly the same turn-partitioning protocol it collapses to 5.9\%, a 64-point loss, worse than the 1-billion-parameter Llama-3.2 model loses in absolute terms and far worse in relative terms. The same model shows the clearest case of blind switching reported in the paper, on MedQA it adopts a newly introduced correct suggestion 47.7\% of the time and an incorrect one 49.2\% of the time, statistically indistinguishable, meaning its turn-2 behavior carries almost no information about whether the new suggestion is actually right. Larger scale does not consistently prevent this failure mode within a family, and the paper separately reports that abstention (declining to answer rather than guessing) degrades even more than accuracy does, and degrades more for the largest models tested, not less, GPT-5.2 loses 21.0 points of correct-abstention rate on MedQA and GPT-4o loses 44.5 points, despite both showing only modest accuracy loss on the same task.

\subsection{Mitigation}
\label{sec:calibration_failure_mitigation}

The intervention that most cleanly helps is conditioning the answer on an explicit, separately maintained estimate of the model's own confidence and carrying it through the conversation, which keeps accuracy roughly flat across repeated pushback where an unmodified baseline degrades~\citep{FirmOrFickleEvaluating_Li}. Explicitly anchoring a model to the correct base rate, telling it the true odds are 50\% in the debate setting, removes the logically impossible mutual overconfidence and lowers the absolute level, though it does not stop the round-over-round drift~\citep{WhenTwoLLMsDebate_TODO}.

Several natural moves backfire. Asking a model to verbalize its confidence, expected to improve calibration, instead accelerates belief erosion under sustained persuasion in one study~\citep{VulnerabilityOfLLMsStated_TODO}. Chain-of-thought and self-reflection do not selectively protect reasoning under multi-turn pressure and can amplify the adoption of wrong suggestions along with right ones~\citep{StopListeningToMe_TODO, EasierToMisleadThan_TODO}. Masking the attention heads that drive flips improves calibration on easy items and worsens overconfidence on hard ones~\citep{InterpretingAndMitigatingUnwanted_TODO}. As elsewhere in this survey, every tested intervention that reduces one failure introduces a cost on another axis, a pattern we take up in \S\ref{sec:discussion}.
